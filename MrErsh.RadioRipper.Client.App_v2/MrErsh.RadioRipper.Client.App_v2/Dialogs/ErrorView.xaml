<Page x:Class="MrErsh.RadioRipper.Client.Dialogs.ErrorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <RelativePanel>
        <Ellipse Fill="Red"
                 Width="40"
                 Height="40"
                 RelativePanel.AlignVerticalCenterWith="Error"
                 RelativePanel.AlignHorizontalCenterWith="Error"></Ellipse>

        <SymbolIcon x:Name="Error"
                    Symbol="Cancel"
                    Width="40"
                    Foreground="White"
                    RelativePanel.AlignVerticalCenterWithPanel="True" />

        <TextBlock Margin="15 0"
                   Text="{Binding}"
                   TextWrapping="Wrap"
                   RelativePanel.RightOf="Error"
                   RelativePanel.AlignVerticalCenterWithPanel="True" />
    </RelativePanel>
</Page>
